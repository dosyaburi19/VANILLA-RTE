/**
 * protected line on delete or backspace key up
 *
 * @param event - key up event
 */
export declare function onDeleteKeyUpProtectedLine(event: KeyboardEvent): void;

/**
 * enrich text
 *
 * @param styleName - css style name (ex: font-size, color)
 * @param style - css style value (ex: 16px, #000000)
 */
export declare function enrichText(styleName: string, style: string): void;
